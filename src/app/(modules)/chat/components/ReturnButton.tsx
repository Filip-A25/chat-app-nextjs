export function ReturnButton() {
  const handleOnClick = () => {};

  return (
    <button
      className="md:hidden rounded-full bg-whitesmoke px-3"
      onClick={handleOnClick}
    >
      ←
    </button>
  );
}
